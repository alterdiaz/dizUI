using System;
using System.Data;

namespace dizUInetcore
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            Console.WriteLine("Apakah anda kampret ?");
            Console.ReadLine();
        }

        private void test()
        {
            System.Data.Sqlclient.SqlDataAdapter sqls = new System.Data.Sqlclient
            String var = "";
            if (var == "")
            {
                var = "";
            }
        }
    }
}
